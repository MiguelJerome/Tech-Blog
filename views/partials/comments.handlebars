<div class="comments">
  {{#each this}}
  <section class="comment" id="comment1" >
    
    <div class="text" id="text1">
      {{comment_text}}

    </div>
    <div class="meta">
        
      {{user.username}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>